Create one dynamic table as shown in the video . Style the webpage as per your requirement but following features must be implement.

1. When one of the input fields are also empty . it should show message as "Empty Input Field(s)" for 2 seconds;
2. After inserting the inputs when it clicks on the button the row should get added.
3. Every row should have a "Remove" button,on clicking which the row should get deleted.
4. After deleting a row it should show a message "Item deleted successfully" for 2 seconds.